import { GoogleSignoutDirective } from './google-signout.directive';

describe('GoogleSignoutDirective', () => {
  it('should create an instance', () => {
    const directive = new GoogleSignoutDirective();
    expect(directive).toBeTruthy();
  });
});
